import 'package:flutter/material.dart';

class KnockoutFixture extends StatelessWidget {
  const KnockoutFixture({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
